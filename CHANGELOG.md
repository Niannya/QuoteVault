# QuoteVault 更新履历

本文记录 QuoteVault 的主要版本变化。README 只介绍当前版本怎么使用；历史版本变化统一放在这里。

当前版本：**0.9.0**

## 0.9.0

- 成员拖动排序或移动群组时，靠近列表顶部/底部可以自动滚动。
- 拖动成员过程中可以直接使用鼠标滚轮滚动成员列表。
- 成员编辑页重新加入“群组”，可直接勾选一个或多个所属群组。
- 保存成员资料时同步保存群组归属，原来的左侧拖动分组方式继续保留。

## 0.8.1

- 删除“从文件管理器直接拖到成员图库/当前图库”的重复导入入口；文件拖入统一保留在右侧“添加”区域。
- 统一截图卡片的拖动触发区，从图片、文字或卡片边框开始拖动时使用同一套逻辑。
- 图库之间普通拖动为“移动”，按住 `Ctrl` 拖动为“复制”。
- 拖出 QuoteVault 到 QQ 等程序时，整个卡片区域都以真实图片文件进行 Windows 拖放，不再出现 JSON 文本。
- 图库中的删除操作区分为“从当前图库移除”和“从所有图库移到回收站”，避免误删其他图库中的关联。

## 0.8.0

- 正式区分“复制到图库”和“移动到图库”。
- 待处理中的截图移动到成员图库后，仍停留在待处理页面，方便连续整理。
- 曾加入从文件管理器直接拖到成员图库/当前图库的入口；该入口在 0.8.1 中因与“添加”区域重复而移除。
- 对图库拖放逻辑进行了集中调整，为后续统一卡片拖动行为做准备。

## 0.7.0

- 修复大图查看器中“复制到剪贴板”的完成提示被查看器遮挡的问题。
- 支持从图库直接把原图拖到 QQ 等 Windows 应用。
- 多选截图后，从其中一张已选截图向外拖动，可以一次拖出全部已选图片。

## 0.6.1

- 收窄大图查看器顶部标题栏和底部工具栏，使布局更接近系统图片查看器。
- 大图查看器新增缩放百分比、缩放滑块以及放大/缩小按钮。
- 支持在大图区域使用鼠标滚轮缩放，缩放范围 10%–400%。

## 0.6.0

- 截图数据由单图库关系升级为多图库关系，同一张截图可以同时属于多个成员图库。
- 编辑截图时可以一次选择多个存放图库。
- 增加针对当前图库的单独移除逻辑，删除某个图库关系不再影响其他图库。
- 右侧预览改为按原图比例完整展示，不再把图片裁进固定高度预览框。
- 编辑页标签输入框移除提示文字。
- 缩略图缓存提高分辨率和 JPEG 质量，改善图库中图片发糊的问题，并自动重建旧缓存。
- 大图查看器重新设计为顶部文件名、中间大图、右侧详情、底部操作的结构。
- 数据结构升级并自动迁移旧版的单图库数据，迁移前会保存备份。

## 0.5.22

- 成员资料中的名称统一称为“ID”，移除重复的自定义 ID 字段。
- 清理成员编辑页多余的提示文本和顶部批量操作图标。
- 清理旧聊天消息编辑器、旧大图查看器样式、冗余成员字段以及演示假数据。
- 删除未使用的 `paddleocr/requirements.txt`。
- 提升数据 Schema，并在升级时自动备份旧索引。

## 0.5.21

- 成员支持框选/多选后批量删除；删除成员不会直接删除原图，无归属截图转入待处理。
- 新增成员资料编辑页，可维护头像、ID、QQ号和备注。
- 成员头像在本地处理并随完整备份保存。
- 左侧成员列表支持显示自定义头像，成员搜索扩展到 ID、QQ号和备注。
- 支持双击成员或通过右键菜单进入成员编辑。

## 0.5.20

- 修复 0.5.19 中成员截图计数函数缺失导致前端初始化异常的问题。
- 恢复图库正常显示，避免界面脚本异常时暴露旧的演示占位内容。

## 0.5.19

- 移除成员图库的自动排序菜单。
- 成员图库改为直接拖动手动排序，并持久化顺序。
- 拖到其他群组中的成员前后时，可以同时调整群组归属和排序位置。

## 0.5.18

- 首次加入成员图库排序菜单，随后在 0.5.19 改为手动拖动排序。
- 改进窗口状态记忆：窗口化关闭后仍以窗口化恢复，最大化关闭后仍以最大化恢复。
- 最大化状态下继续保留最后一次窗口化的位置和大小。

## 0.5.17

- 调整窗口化时的批量操作栏，把图片操作稳定分组到右侧，减少窄窗口换行错位。
- 截图框选可以从图库左右空白区域开始。
- 左侧成员树扩展可框选空白区域，方便从成员列表下方开始框选。

## 0.5.16

- 恢复自适应网格填充，减少隐藏左侧图库后的大片空白。
- 框选改为基于内容坐标固定起点，滚动过程中选框行为更接近资源管理器。
- 批量工具栏改为固定预留空间和柔和显示，减少界面跳动。
- 大图查看器的复制按钮调整到详情区域右下角。

## 0.5.15

- 截图框选增加边缘自动滚动和跨屏选择。
- 缩略图加载失败时回退显示原图。
- 改善成员多选和拖放反馈。

## 0.5.14

- 截图框选改为始终可用，不需要提前进入专门模式。
- `Ctrl` 支持追加选择。
- 批量操作增加“取消全选/反选”等选择控制。

## 0.5.13

- 调整收藏优先开关与文字的对齐。
- 统一自定义提示文本，移除收藏优先和分隔线等不必要提示。
- 缩略图密度滑块提示统一为“调整缩略图密度”。

## 0.5.12

- 默认全局收录快捷键由 `Ctrl+Alt+Q` 改为 `Ctrl+Alt+V`，旧默认值会自动迁移。
- 修复窗口化状态下缩略图密度滑块失效的问题。
- 修复右侧工作区分隔线拖动错误。
- 新功能按钮逐步统一为应用内自定义提示，不再依赖浏览器原生 `title`。

## 0.5.11

- 取消此前实验性的双布局，只保留统一网格图库。
- 增加三档缩略图密度滑块，实际列数根据窗口宽度自动变化。
- 大图查看器不再提供“原始大小”切换，默认完整适应窗口。
- 大图查看器加入“复制到剪贴板”。
- 左侧图库加入隐藏/恢复按钮，并保存隐藏状态。
- 右侧工作区针对窗口化缩窄并修复分隔线宽度恢复问题。

## 0.5.10

- 重做早期大图查看器，采用图片 + 详情的结构。
- 添加页和编辑页开始统一完整比例图片预览。
- 加入截图收藏，以及“收藏优先”排序开关。
- 当时仍保留极简/详尽两种实验布局，后续在 0.5.11 统一为网格布局。

## 0.5.7–0.5.9

- 集中改善右侧面板切换、窗口调整时图库反复重绘和闪烁的问题。
- 编辑页增加完整截图显示。
- 扩展大图查看器，并让预览、编辑、添加页中的图片都能进入大图查看。
- 增加图库双击和右键查看大图的交互。

## 0.5.2–0.5.6

- 主要进行窗口化布局、图库表现和左右工作区尺寸的多轮探索。
- 右侧工作区逐步收窄到更适合窗口化使用的范围。
- 编辑截图开始支持调整存放图库。
- 尝试过极简/详尽、相册式布局等方案，最终在后续版本中收敛为统一网格布局。
- 开始加入成员框选等后续批量整理功能的基础交互。

## 0.5.1

- 改善窗口化状态下批量工具栏的排列。
- 统一备份过程中的进度和完成提示。
- 加入一键生成 Windows 发布包的 CMD 脚本，并修正脚本编码/换行兼容性。

## 0.5.0

- 清理早期逐消息、说话人昵称、别名等已经不再使用的旧数据结构。
- 将截图模型收敛为“图库关系 + 可搜索文本 + 标签 + 可选 OCR”。
- 建立新的数据 Schema 基线和迁移规则。
- 项目继续使用 `.NET 9` / `net9.0-windows`。

## 0.4.7 及更早

0.4.7 是 GitHub 仓库在本轮大规模迭代前的旧基线版本。更早版本属于早期开发阶段，数据和界面模型与 0.5.0 之后差异较大，因此本履历不再逐个补记。
